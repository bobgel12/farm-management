# Local frontend against the deployed production backend
# Copy to .env.frontend-prod if you need machine-specific overrides.

FRONTEND_HOST_PORT=3002
REACT_APP_API_URL=https://farm-management-production-54e4.up.railway.app/api
CHOKIDAR_USEPOLLING=true
NODE_OPTIONS=--max-old-space-size=4096
GENERATE_SOURCEMAP=false
